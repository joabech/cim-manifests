# Python package dependencies for different SDK profiles
# Used by sdk-manager for installation and Docker generation

profiles:
  # Only supported profile right now
  docs:
    packages:
      - sphinx
      - sphinx-rtd-theme
      - myst-parser
      - sphinx-autobuild
      - kconfiglib

default: docs
